#include <stdio.h>
/**
 * main - possible combo of two digits
 * Return:0 (success)
 */
int main(void)
{
	int x = 0;

	while
		(x < 99);
	{
		putchar (x + '0');
		if (x < 99)
		{
			putchar (',');
			putchar (' ');
		}
		x++;
	}
	putchar ('\n');

	return (0);
}
