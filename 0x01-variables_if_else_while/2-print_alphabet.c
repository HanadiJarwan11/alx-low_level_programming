#include <stdio.h>

/**
 * main - display alphabets
 * Return: 0 (success)
 */
int main(void)
{
	char letters = 'a';
i
	while (letters <= 'z')
	{
		printf("%c\n", letters);
		letters++;
	}
	return (0);
}
