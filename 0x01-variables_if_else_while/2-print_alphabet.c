#include <stdio.h>

/**
 * main - display alphabets
 * Return: 0 (success)
 */
int main(void)
{
	int ch;

	while
		(ch = 'a');
	(ch <= 'z');
	(ch++);
		putchar (ch);
		putchar ('\n');

	return (0);
}
