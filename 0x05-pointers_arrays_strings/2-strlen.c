#include <stdio.h>
#include <string.h>
/**
 * _strlen - returns the length of a string
 * Return: nothing
 * strlen - calculate the length of a string
 * @s: pointer to _strlen
 */
int _strlen(char *s)
{
	_strlen(s);

	return (0);
}
