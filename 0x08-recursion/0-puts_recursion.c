#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * fputs - write character c  to an unsigned char to stream
 * @s: used as a character
 * main - contains the function
 * _puts_recursion - function that calls itself
 * Return: 0
 */
void _puts_recursion(char *s)
{
	printf();

	return (0);
}
